{% extends 'base.html.twig' %}

{% block title %}Instrument{% endblock %}

{% block body %}
    <h1>Instrument</h1>

    <table class="table">
        <tbody>
            <tr>
                
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ instrument.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ instrument.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>
                        <img src="/upload/{{instrument.image}}">
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instrument_index') }}">back</a>
    <br>
    <a class="btn btn-warnin" href="{{ path('instrument_edit', {'id': instrument.id}) }}">edit</a>

    {{ include('instrument/_delete_form.html.twig') }}
{% endblock %}
